#include<stdio.h>
int main()
{
	int n,d,c=0;
	printf("Enter any no ");
	scanf("%d",&n);
	while(n>0)
	{
		d=n%10;
		if(d%2==0)
			c=c+1;
		n=n/10;
	}
	printf("Count of even digits  = %d\n",c);
	return 0;
	
}
